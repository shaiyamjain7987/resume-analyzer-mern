{
  "name": "ai-resume-analyzer-mern",
  "version": "1.0.0",
  "private": true,
  "description": "MERN based AI resume analyzer",
  "scripts": {
    "dev": "npm run build  \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server run start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
